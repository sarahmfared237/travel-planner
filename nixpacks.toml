[phases.setup]
nixPkgs = ["maven", "jdk21_headless"]

[phases.build]
cmds = ["./mvnw -B -DskipTests -Dmaven.compiler.release=21 clean install -Pproduction"]

[start]
cmd = "java -jar target/travel-planner-0.0.1-SNAPSHOT.jar"
